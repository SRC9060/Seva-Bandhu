# Seva Bandhu Frontend

This folder is reserved for a future standalone frontend.

The current user interface is still implemented with Django templates in `../backend/core/templates`. Those templates were preserved to avoid changing application behavior during the repository cleanup.
